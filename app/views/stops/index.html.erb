<div class="hp">
  <div id="stops_index_top">
    <h2>Stop list for the <%= @tour.name%></h2>
    <p>Load your stops for the <%= @tour.name%> tour by importing a CSV File.</p>
    <p>If you have not done this before, you may want to use the provided template:</p><%= link_to 'Download template', stops_download_template_url %>
    <br />
    <br />

    <p>If you've already added your Stop information to the template, use the buttons below to choose your file and upload it.</p>
    <div class="choose_file">
      <%= form_tag import_organization_tour_stops_path, multipart: true do %>
      <%= file_field_tag :file %>
      <br />
      <%= submit_tag " Import CSV ", class="stop-btn" %>
      <% end %>
    </div>
    <br />

    <p>Use the buttons below to download your tour stops in either CSV or xls format.</p>
    <div class="button_div">
      <%= button_to "   CSV   ", organization_tour_stops_path(format: "csv"), {:id => "button_left" } %> | <%= button_to "  Excel  ", organization_tour_stops_path(format: "xls"), {:id => "button_right" } %>
    </div>
    <br />
  </div>

  <%= button_to 'Delete All Stops', destroy_stops_organization_tour_stops_path(@organization.id), method: :delete, data: { confirm: 'This will delete ALL stops associated with this tour. Are you sure?' } %>
  <br />

<% if @stops.blank? %>
<% else %>
  <table>
    <thead>
      <tr>
        <th>Stop Number</th>
        <th>Stop Name</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
    </thread>
    <tbody>
      <% @tour.stops.sort_by { |stop| stop.stop_num }.each do |stop| %>
        <tr>
          <td><%= stop.stop_num %></td>
          <td><%= stop.name %></td>
          <td><%= link_to 'See stop details', organization_tour_stop_path(@organization.id, @tour.id, stop.stop_num) %></td>
          <td><%= link_to 'Edit stop details', edit_organization_tour_stop_path(@organization.id, @tour.id, stop.stop_num) %></td>
          <td><%= link_to 'Delete stop', organization_tour_stop_path(@organization.id, @tour.id, stop.stop_num), method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>

</div>
<br />
<br />
